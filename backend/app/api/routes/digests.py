# Digest routes

from fastapi import APIRouter

router = APIRouter(prefix="/api/v1/digests", tags=["digests"])

# TODO: Implement endpoints
# - GET /digests
# - GET /digests/{id}
# - GET /digests/user/{user_id}
# - POST /digests (admin only)
