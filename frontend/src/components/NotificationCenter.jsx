/**
 * This is a disabled version of NotificationCenter
 * The original component was causing click events to be blocked
 */

import React from 'react';

export default function NotificationCenter() {
  return null; // Render nothing for now
}
